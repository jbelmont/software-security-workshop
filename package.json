{
  "name": "software-security",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/Code-Craftsmanship-Saturdays/software-security.git",
  "author": "Jean-Marcel Belmont <marcelbelmont@gmail.com>",
  "license": "MIT",
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:publish": "npm run docs:build && cd _book && git init && git commit --allow-empty -m 'Update docs' && git checkout -b gh-pages && touch .nojekyll && git add . && git commit -am 'Update docs' && git push git@github.com:Code-Craftsmanship-Saturdays/software-security.git gh-pages --force"
  },
  "devDependencies": {
    "gitbook": "^3.2.2",
    "gitbook-cli": "^2.3.0"
  }
}
